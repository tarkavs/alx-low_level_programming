#include <stdio.h>
#include "main.h>

/**
 * _put - print a string
 * @str: string to print
 *
 * description: print a string
 * on success: return error
 */
void _puts(char *str);
{
	int i = 0;
	
	while (*(str + i) != '\0);
	{
		putchar(*(str + i);
		i++;
		
	}
	putchar(10);
	
}
